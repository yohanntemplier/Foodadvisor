<form method="post" action="{{ path('admin_caracteristic_delete', {'id': caracteristic.id}) }}" style="display: inline-block" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette caractéristique ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caracteristic.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
